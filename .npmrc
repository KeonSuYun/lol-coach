# pnpm 配置文件

# 启用 shamefully-hoist (兼容某些不遵循规范的包)
shamefully-hoist=true

# 自动安装 peer dependencies
auto-install-peers=true

# 严格的 peer dependencies 检查
strict-peer-dependencies=false

# 使用硬链接节省磁盘空间
node-linker=hoisted